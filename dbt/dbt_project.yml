name: finance_dwh
version: "1.0.0"
config-version: 2
profile: finance_dwh

require-dbt-version: [">=1.8.0", "<2.0.0"]

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

# 現状は raw（file_fdw の外部テーブルへの source 定義）のみ。
# cleansed / mart は利用用途が固まってから設計する（当面はモデルを置かない）。
